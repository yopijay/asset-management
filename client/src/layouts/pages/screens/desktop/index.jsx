// Libraires
import { Stack } from "@mui/material";

// Custom styles
const container = {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    alignItems: 'stretch',
    padding: '0 20px',
    height: '100vh',
    overflow: 'hidden'
}
const Index = () => {
    return (
        <Stack sx= { container }>
            
        </Stack>
    );
}

export default Index;